# =============================================================================
# Algotrading Stack - Environment Variables / Variables de Entorno
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Copie este archivo a .env y complete los valores:
#   cp .env.example .env
# =============================================================================

# --- Directories / Directorios -----------------------------------------------
# Working directory where this project resides
# Directorio de trabajo donde reside este proyecto
WD=/path/to/your/algotrading

# Notebooks directory accessible from containers
# Directorio de notebooks accesible desde los contenedores
ND=/path/to/your/notebooks

# --- Timezone / Zona horaria -------------------------------------------------
TZ=America/New_York

# --- Airflow ------------------------------------------------------------------
AIRFLOW_UID=1000
AIRFLOW_GID=0

# Airflow web UI credentials (first-time setup)
# Credenciales de la UI web de Airflow (primera configuracion)
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=change_me_airflow_password

# Fernet key for encrypting connections/variables (generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
# Clave Fernet para cifrar conexiones/variables
AIRFLOW_FERNET_KEY=<generate_your_fernet_key>

# JWT secret for API authentication (generate with: python -c "import secrets; print(secrets.token_urlsafe(32))")
# Secreto JWT para autenticacion de la API
AIRFLOW_JWT_SECRET=<generate_your_jwt_secret>

# --- PostgreSQL (Airflow metadata) --------------------------------------------
# Credentials for the Airflow metadata database
# Credenciales para la base de datos de metadata de Airflow
PG_AIRFLOW_USER=airflow
PG_AIRFLOW_PASSWORD=change_me_pg_airflow_password
PG_AIRFLOW_DB=airflow

# --- PostgreSQL (Master / Data Warehouse) -------------------------------------
# Credentials for the main trading data warehouse
# Credenciales para el data warehouse principal de trading
PG_MASTER_PASSWORD=change_me_pg_master_password
PG_MASTER_SHARED_PASSWORD=change_me_shared_password

# --- pgAdmin ------------------------------------------------------------------
# Web UI for PostgreSQL administration
# UI web para administracion de PostgreSQL
PGADMIN_EMAIL=admin@yourdomain.com
PGADMIN_PASSWORD=change_me_pgadmin_password

# --- Interactive Brokers Gateway ----------------------------------------------
# IB Gateway credentials and configuration
# Credenciales y configuracion del Gateway de IB
TWSUSERID=your_ib_username
TWSPASSWORD=your_ib_password
TRADING_MODE=paper
VNC_PASSWORD=change_me_vnc_password

# --- MetaTrader 5 -------------------------------------------------------------
# Web access credentials (leave PASSWORD empty to disable authentication)
# Credenciales de acceso web (dejar PASSWORD vacio para deshabilitar autenticacion)
CUSTOM_USER=your_mt5_username
PASSWORD=your_mt5_password

# --- Metabase -----------------------------------------------------------------
# Business Intelligence platform credentials
# Credenciales de la plataforma de Business Intelligence
MB_DB_USER=metabase_user
MB_DB_PASS=change_me_metabase_password

# Encryption key for Metabase (generate with: python -c "import secrets; print(secrets.token_hex(32))")
# Clave de cifrado para Metabase
MB_ENCRYPTION_SECRET_KEY=change_me_metabase_encryption_key
